# Inheritance-Implementation
A base class 'Animal' with common attributes like name and age is created and then, derived classes 'Dog' and 'Cat' is created that inherit from 'Animal' and added specific attributes and methods. Also, polymorphism is demonstrated by creating an array of 'Animal' objects and calling a common method (e.g., 'makeSound').
